package app

// handleWork handles the !work command.
// It should add a random amount of currency to the user's balance between 1 and 10.
// It should notifiy the user of the amount they earned.
func handleWork(a *App, m *Message) error {
	return nil
}
